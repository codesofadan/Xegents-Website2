import { ServicesPageClient } from "./services-client"

export const metadata = {
  title: "Services | Xegents",
  description:
    "AI audits, automation systems, custom AI agents, and system integration — Xegents finds the workflows bleeding time and money, then builds the fix.",
}

export default function ServicesPage() {
  return <ServicesPageClient />
}
